.build/
.DS_Store
spike/imuspike
spike/imuspike2
spike/batchtest
*.o
# dist/ build output only; the scripts and plist that make it are tracked
dist/Tunk.app/
dist/*.dmg
dist/dmg-staging/
.build-*/

# harness scratch, regenerated by tunk-score selftest
.tunk-*/
design/png/
design/AppIcon.iconset/

# Workflow agent worktrees — transient, and each is its own git repo.
.claude/worktrees/
